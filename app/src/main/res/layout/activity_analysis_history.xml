<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1A2E">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Common Header -->
        <include layout="@layout/layout_common_header" />

        <!-- Table container: header row + data rows share the same HorizontalScrollView -->
        <HorizontalScrollView
            android:id="@+id/hsvTable"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <!-- Sticky column header row (populated programmatically) -->
                <LinearLayout
                    android:id="@+id/tableHeaderRow"
                    android:layout_width="wrap_content"
                    android:layout_height="48dp"
                    android:orientation="horizontal"
                    android:background="#252542"
                    android:gravity="center_vertical" />

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:background="#33FFFFFF" />

                <!-- Data rows via RecyclerView -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvAnalysisTable"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:clipToPadding="false"
                    android:paddingBottom="24dp" />

            </LinearLayout>
        </HorizontalScrollView>

    </LinearLayout>

    <!-- Empty state overlay -->
    <LinearLayout
        android:id="@+id/emptyState"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@android:drawable/ic_menu_recent_history"
            android:alpha="0.4"
            app:tint="#888888" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No analyses recorded yet"
            android:textColor="#AAAAAA"
            android:textSize="17sp"
            android:textStyle="bold"
            android:layout_marginTop="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Complete a gait analysis to see results here"
            android:textColor="#666666"
            android:textSize="13sp"
            android:layout_marginTop="6dp" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
